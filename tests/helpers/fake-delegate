#!/usr/bin/env bash
set -euo pipefail
: "${FAKE_CAPTURE_DIR:?}"
printf '%s' "$(basename -- "$0")" > "$FAKE_CAPTURE_DIR/command"
printf '%s' "$#" > "$FAKE_CAPTURE_DIR/count"
index=0
for argument in "$@"; do
  printf '%s' "$argument" > "$FAKE_CAPTURE_DIR/arg-$index"
  index=$((index + 1))
done
